/** Base API address (json-server), used in "http" mode. */
export const API_URL = "http://localhost:3333";

/** Resource name in server.json. */
export const RESOURCE = "schedules";

/** localStorage key used in "local" mode. */
export const STORAGE_KEY = "pet-world:schedules";
